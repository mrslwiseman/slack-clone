import React from 'react'

const Footer = (props) => {
    return (
        <footer className="app__footer">
          <p>Simple Slack clone by Matt Wiseman.</p>
          <p>Built with React + SASS.</p>
      </footer>
    )
}

export default Footer;

