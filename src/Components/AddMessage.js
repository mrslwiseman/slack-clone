import React from 'react'

const AddMessage = (props) => {
    return (
        <footer>
            <form>
                <input type="text" />
                <button>Send</button>
            </form>
        </footer>
    )
}

export default AddMessage;